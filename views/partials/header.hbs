<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top justify-content-between">
    <a class="navbar-brand" href="/">{{ projectName }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {{#if navView}}active{{else}}{{/if}}" href="/works/list">View All</a>
                </li>
            {{#if loggedIn}}
                <li class="nav-item">
                    <a class="nav-link {{#if navAdd}}active{{else}}{{/if}}" href="/works/add">
                        Add a Work <!--<i data-feather="file-plus"></i>-->
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/users/profile">Profile</a>
                </li>
                <!--TODO Logged in as: currentUser.username-->
            {{else}}
                <li class="nav-item">
                    <a class="nav-link {{#if navRegister}}active{{else}}{{/if}}" href="/users/register">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if navLogin}}active{{else}}{{/if}}" href="/users/login">Login</a>
                </li>
            {{/if}}
        </ul>
    </div>
</nav>

<h1>{{ title }}</h1>